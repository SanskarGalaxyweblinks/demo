# Database Config (Keep your existing DB config if you have it)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/jupiter_demo

# Email Configuration
# The code looks for variables starting with SMTP_, so we rename them here.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ag20020804@gmail.com
# IMPORTANT: Password with spaces must be in quotes
SMTP_PASS="gnlf ossk obok eddn"